# Environment
.env
.env.local
.env.*.local
.env.production

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# Secrets
*.pem
keys/
secrets/

# Claude (share workflow commands; keep machine-local settings out)
.claude/*
!.claude/commands/

# planning/ is tracked (workflow handoff docs shared so the wf-* flow is portable)
PR.md
node_modules

# Exception: the security-tools roadmap and its sessions commit code only (no planning
# .md), so their planning docs stay local-only and uncommitted.
planning/security-tools
planning/ascii-sum-tool
planning/enigma-engine
planning/enigma-ui
